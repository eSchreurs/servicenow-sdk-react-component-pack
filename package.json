{
  "name": "servicenow-sdk-react-component-pack-monorepo",
  "version": "1.0.0",
  "description": "Monorepo — ServiceNow React Component Pack (scoped app + NPM component library)",
  "license": "UNLICENSED",
  "private": true,
  "workspaces": [
    "servicenow-app",
    "npm-package"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "deploy": "npm run deploy --workspace=servicenow-app"
  }
}
